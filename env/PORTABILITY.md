# Environment Variables

Validate your .env variables to ensure correct configuration.

## Portability

1. Install `@t3-oss/env-nextjs`.

2. Copy `env` folder.

3. Update your environment variables in `env/index.ts`.
4. Add:

- `.env`
- `.env.staging`
- `.env.prod`
